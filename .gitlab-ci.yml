# 极狐 GitLab Pages 部署配置
pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp auto-evaluate.user.js public/
    - echo "已自动将脚本复制到 public 目录，准备部署 Pages..."
  artifacts:
    paths:
      - public
  only:
    - main
    - master
